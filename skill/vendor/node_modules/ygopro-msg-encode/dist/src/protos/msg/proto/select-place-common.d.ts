import { YGOProMsgResponseBase } from '../with-response-base';
export interface SelectablePlace {
    player: number;
    location: number;
    sequence: number;
}
export declare class YGOProMsgSelectPlaceCommon extends YGOProMsgResponseBase {
    player: number;
    count: number;
    flag: number;
    getSelectablePlaces(): SelectablePlace[];
    responsePlayer(): number;
    prepareResponse(places: Array<{
        player: number;
        location: number;
        sequence: number;
    }>): Uint8Array<ArrayBuffer>;
}
