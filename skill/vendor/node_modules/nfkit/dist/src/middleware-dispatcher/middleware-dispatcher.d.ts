import { AnyFunc, Middleware } from './types';
import { DynamicMiddlewareDispatcher } from './dynamic-middleware-dispatcher';
export declare class MiddlewareDispatcher<F extends AnyFunc> extends DynamicMiddlewareDispatcher<F> {
    middlewares: Middleware<F>[];
    middleware(mw: Middleware<F>, prior?: boolean): this;
    removeMiddleware(mw: Middleware<F>): this;
    buildMiddlewares(...args: Parameters<F>): Promise<Middleware<F>[]>;
}
