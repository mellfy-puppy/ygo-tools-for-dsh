import { Empty } from '../types';
import { AppContext, AppServiceClass, AppProvideArgs, AppProvidedMerged, AppContextUsed } from './types';
export declare class AppContextCore<Cur = Empty, Req = Empty> {
    private __current;
    private __required;
    private provideRecords;
    private registry;
    private objectSteps;
    started: boolean;
    private starting;
    private settleCursor;
    private recordEntries;
    private pendingInitQueue;
    private pendingInitSet;
    private initializedEntries;
    private loadingRecords;
    private settlementChain;
    private settlementError;
    private settling;
    private assertSettlementHealthy;
    private enqueueInit;
    private sortInitQueueByDeclarationOrder;
    private constructPendingRecords;
    private settlePending;
    private scheduleSettlement;
    private triggerSettlement;
    private findProvideRecord;
    private createEntryFromRecord;
    provide<C extends AppServiceClass<Cur, Req>, const P extends string = '', const M extends (keyof InstanceType<C>)[] = []>(cls: C, ...args: AppProvideArgs<Cur, Req, C, P, M>): AppProvidedMerged<Cur, Req, C, P, M>;
    get<R>(cls: AppServiceClass<Cur, Req, any, R> | (() => AppServiceClass<Cur, Req, any, R>)): R;
    getAsync<R>(cls: AppServiceClass<Cur, Req, any, R> | (() => AppServiceClass<Cur, Req, any, R>)): Promise<R>;
    use<const Ctxes extends AppContext<any, any>[]>(...ctxes: Ctxes): AppContextUsed<Cur, Req, Ctxes>;
    define(): AppContext<Cur, Req>;
    start(): Promise<Empty extends Req ? AppContext<Cur, Req> : never>;
}
export declare const createAppContext: <Req = Empty>() => AppContext<Empty, Req>;
