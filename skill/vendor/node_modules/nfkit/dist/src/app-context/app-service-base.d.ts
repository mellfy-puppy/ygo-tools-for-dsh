import { Empty } from '../types';
import { AppContext } from './types';
export declare class AppServiceBase<Cur = Empty, Config extends any[] = []> {
    ctx: AppContext<Cur>;
    constructor(ctx: AppContext<Cur>, ...args: Config);
    init(): Promise<void>;
}
