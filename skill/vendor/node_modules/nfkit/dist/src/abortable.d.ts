export declare class AbortedError extends Error {
    cause?: unknown;
    name: string;
    constructor(msg?: string, cause?: unknown);
}
export interface AbortableOpts {
    /** 是否把原始值装箱后再代理（会改变 typeof/=== 语义，默认 false） */
    boxPrimitives?: boolean;
    /** 不递归代理子对象（仅代理顶层对象/函数，默认 false） */
    noRecursive?: boolean;
}
export declare function abortable<T>(obj: T, signal: AbortSignal, opts?: AbortableOpts): T;
